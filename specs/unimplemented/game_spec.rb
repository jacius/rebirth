
describe "game" do
  
  it "should have a default view"
  it "should have only one view as active"

  it "should have a default world"
  it "should have only one world as active"

  it "should have a hash namespace"
  it "should accept strings or symbol names as keys in namespace"
  it "should accept any sort of object as values in namespace"

  it "should have game objects as children"
  it "should be able to add game objects anonymously as args"
  it "should be able to add game objects as :name => object pairs"

  it "should have a hash namespace for objects"
  it "should put named objects into the object namespace when added"

  it "should have event hooks"

end
